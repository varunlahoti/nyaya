# Minimal deps for the DEPLOYED Streamlit app (Streamlit Community Cloud installs
# this file — it's next to streamlit_app.py). Kept small + unpinned-upper so it
# gets prebuilt wheels on whatever Python the host uses (incl. 3.14), avoiding
# slow/failing source builds.
#
# The full FastAPI backend deps are in requirements-api.txt.
streamlit>=1.40,<2
pydantic>=2.11,<3
pydantic-settings>=2.7,<3
httpx>=0.27,<1

# Hosted corpus (Neon pgvector) — needed only when VECTOR_BACKEND=postgres.
# asyncpg ships C-extension wheels; if the Streamlit Cloud build fails to find a
# wheel, set the app's Python version to 3.12 or 3.13 (Advanced settings) where
# wheels exist. Harmless when VECTOR_BACKEND is unset (imported lazily).
SQLAlchemy>=2.0,<2.1
asyncpg>=0.30
